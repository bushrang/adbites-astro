---
interface Props {
    data: {
        headline?: string;
        subline?: string;
        logos?: {
            imageUrl: string;
            altText?: string;
        }[];
    };
}

const { data } = Astro.props;

if (!data || !data.logos || data.logos.length === 0) {
    return null;
}
---

<section class="py-24 bg-transparent px-4 relative z-20">
    <div class="max-w-6xl mx-auto text-center relative">
        
        {data.headline && (
            <h2 class="text-3xl md:text-5xl font-heading font-bold text-white mb-4 tracking-tight drop-shadow-md">
                {data.headline}
            </h2>
        )}
        
        {data.subline && (
            <p class="text-lg md:text-xl text-slate-300 font-light max-w-2xl mx-auto mb-16">
                {data.subline}
            </p>
        )}

        <div class="flex flex-wrap justify-center items-center gap-6 md:gap-10 max-w-5xl mx-auto">
            {data.logos.map((logo) => (
                <div class="flex items-center justify-center w-28 h-20 sm:w-36 sm:h-24 p-3 sm:p-4 opacity-60 grayscale hover:grayscale-0 hover:opacity-100 transition-all duration-500 ease-in-out cursor-default transform hover:scale-105">
                    <img 
                        src={logo.imageUrl} 
                        alt={logo.altText || "Client Logo"} 
                        loading="lazy" 
                        class="max-w-full max-h-full object-contain drop-shadow-md"
                    />
                </div>
            ))}
        </div>
        
    </div>
</section>
